const Headlines = () => {
  return (
    <div>
      {/* {HeadlineNews.map((headline, index) => {
        <div key={index}>
          <h1>{headline.title}</h1>
          <p>{headline.categoryName}</p>
        </div>;
      })} */}
      News Headlines will appear here.
    </div>
  );
};

export default Headlines;
